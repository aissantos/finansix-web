# =============================================================================
# Finansix Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Get these from your Supabase project dashboard:
# Dashboard > Settings > API

# The URL of your Supabase project
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# The ANON (public) key - safe to expose in client-side code
# SECURITY: This key is safe ONLY if you have RLS (Row Level Security) enabled!
# Without RLS, anyone with this key can read/write your entire database!
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# -----------------------------------------------------------------------------
# Error Tracking (Optional)
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking in production
VITE_SENTRY_DSN=

# -----------------------------------------------------------------------------
# CRITICAL SECURITY CHECKLIST
# -----------------------------------------------------------------------------
# Before going to production, ensure:
#
# 1. [ ] RLS is ENABLED on ALL tables
#    - Go to Supabase Dashboard > Database > Tables
#    - Each table should show "RLS enabled" badge
#
# 2. [ ] RLS Policies are configured
#    - Run the migration: supabase/migrations/20240105000002_rls_policies.sql
#    - Test by trying to access data without authentication
#
# 3. [ ] The ANON key is NOT the SERVICE_ROLE key
#    - ANON key starts with "eyJ..." and is ~200 chars
#    - SERVICE_ROLE key is longer and should NEVER be in frontend code
#
# 4. [ ] .env.local is in .gitignore
#    - Verify: git status should NOT show .env.local
#
# To test RLS is working:
#   1. Open browser console on your app
#   2. Run: await supabase.from('transactions').select('*')
#   3. You should ONLY see YOUR household's data, not others'
# =============================================================================
